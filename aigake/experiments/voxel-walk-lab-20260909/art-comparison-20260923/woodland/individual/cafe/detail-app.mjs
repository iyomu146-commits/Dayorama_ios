import * as THREE from 'three';
import {OrbitControls} from 'three/addons/controls/OrbitControls.js';
import {createCafeCells,CELL} from './model.mjs?v=finish3';
import {PALETTE,PART_NAMES} from './palette.mjs?v=finish3';
import {buildCafeSurfaces,installSurfaceShader} from './surface-finish.mjs?v=finish3';
const $=id=>document.getElementById(id),cells=createCafeCells({stage:'detail'}),root=new THREE.Group();root.name='cafe';
const scene=new THREE.Scene(),camera=new THREE.OrthographicCamera(-5,5,4,-4,.1,200),renderer=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true,alpha:true});
renderer.setSize(980,754,false);renderer.setPixelRatio(1);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFShadowMap;$('view').append(renderer.domElement);scene.add(root);
const key=new THREE.DirectionalLight('#fff0d8',2.8),hemi=new THREE.HemisphereLight('#e9f2f5','#a59a7d',1.65),fill=new THREE.DirectionalLight('#d9e9ec',.6);key.position.set(-8,16,14);fill.position.set(10,10,-10);key.castShadow=true;key.shadow.mapSize.set(2048,2048);key.shadow.radius=2.5;Object.assign(key.shadow.camera,{left:-10,right:10,top:10,bottom:-10,near:.1,far:50});key.shadow.bias=-.0001;key.shadow.normalBias=.018;scene.add(key,hemi,fill);
const floor=new THREE.Mesh(new THREE.PlaneGeometry(200,200),new THREE.ShadowMaterial({color:'#756956',opacity:.14}));floor.rotation.x=-Math.PI/2;floor.position.y=-.003;floor.receiveShadow=true;scene.add(floor);
// Procedural environment: independent from cell albedo; no source-photo projection.
const envScene=new THREE.Scene(),envGeo=new THREE.SphereGeometry(50,32,16),envColors=[];
for(let i=0;i<envGeo.attributes.position.count;i++){const y=envGeo.attributes.position.getY(i)/50,c=new THREE.Color('#eee6d0').lerp(new THREE.Color(y>0?'#bdd4da':'#7f8974'),Math.abs(y));envColors.push(c.r,c.g,c.b);}
envGeo.setAttribute('color',new THREE.Float32BufferAttribute(envColors,3));const envMat=new THREE.MeshBasicMaterial({vertexColors:true,side:THREE.BackSide});envScene.add(new THREE.Mesh(envGeo,envMat));const pmrem=new THREE.PMREMGenerator(renderer),env=pmrem.fromScene(envScene,.03,.1,100);scene.environment=env.texture;scene.environmentIntensity=.25;pmrem.dispose();envGeo.dispose();envMat.dispose();
const palette=Object.fromEntries(Object.entries(PALETTE).map(([id,c])=>{const v=new THREE.Color(c);return[id,[v.r,v.g,v.b]];}));
const material=new THREE.MeshStandardMaterial({vertexColors:true,roughness:.9});installSurfaceShader(material);let finishMode='texture';
const clayMaterial=new THREE.MeshStandardMaterial({color:PALETTE.clay,roughness:.9}),maskMaterial=new THREE.MeshBasicMaterial({color:'#696969'});let triangles=0;const boxes=new Map();
function geometry(m){const g=new THREE.BufferGeometry();for(const [name,data,n] of [['position',m.positions,3],['normal',m.normals,3],['color',m.colors,3],['surface',m.surfaces,2]])g.setAttribute(name,new THREE.BufferAttribute(data,n));g.computeBoundingBox();g.computeBoundingSphere();return g;}
function rebuild(){for(const m of root.children){m.geometry.dispose();m.userData.explodedGeometry?.dispose();}root.clear();boxes.clear();triangles=0;for(const m of buildCafeSurfaces(cells,palette,{finish:finishMode!=='grid'})){const g=geometry(m),mesh=new THREE.Mesh(g,material);mesh.name=m.part;mesh.castShadow=mesh.receiveShadow=true;root.add(mesh);triangles+=m.triangles;boxes.set(m.part,g.boundingBox.clone());}material.userData.surfaceAmount.value=finishMode==='texture'?1:0;}
rebuild();
root.userData.sculptRuntime={parts:root.children.map(m=>({id:m.name,meshName:m.name,pivot:boxes.get(m.name).getCenter(new THREE.Vector3()).toArray()})),colliders:root.children.map(m=>({part:m.name,min:boxes.get(m.name).min.toArray(),max:boxes.get(m.name).max.toArray()})),stage:'detail prototype; formal visual gates pending',construction:'integer 15cm voxels'};
const controls=new OrbitControls(camera,renderer.domElement);controls.enabled=false;controls.enableDamping=false;controls.enablePan=false;let interactive=false,masked=false,clay=false,exploded=false;
const isMain=id=>!['terrace','table','chair-front','chair-back','cup'].includes(id)&&!id.startsWith('plant-');
function draw(){renderer.render(scene,camera);if($('status'))$('status').textContent=`${cells.size.toLocaleString()}ボクセル · ${root.children.reduce((n,m)=>n+m.geometry.attributes.position.count/3,0).toLocaleString()}三角形 · ${root.children.length}部材`;}
function fit(name='match'){
 interactive=false;controls.enabled=false;$('orbit').setAttribute('aria-pressed','false');const v=({match:[13.5,11.5,30],front:[0,10,30],right:[30,10,0],back:[0,10,-30],left:[-30,10,0]})[name];camera.position.set(...v);camera.lookAt(0,0,0);camera.updateMatrixWorld();
 const right=new THREE.Vector3().setFromMatrixColumn(camera.matrixWorld,0),up=new THREE.Vector3().setFromMatrixColumn(camera.matrixWorld,1);let x0=Infinity,x1=-Infinity,y0=Infinity,y1=-Infinity;
 for(const mesh of root.children){if(name==='match'&&!exploded&&!isMain(mesh.name))continue;const a=mesh.geometry.attributes.position;for(let i=0;i<a.count;i++){const p=new THREE.Vector3().fromBufferAttribute(a,i).add(mesh.position),x=p.dot(right),y=p.dot(up);x0=Math.min(x0,x);x1=Math.max(x1,x);y0=Math.min(y0,y);y1=Math.max(y1,y);}}
 const ratio=490/377,matched=name==='match'&&!exploded,height=Math.max((x1-x0)/(ratio*(matched?370/490:.86)),(y1-y0)/(matched?349/377:.86)),width=height*ratio,target=right.clone().multiplyScalar((x0+x1)/2-(matched?(198/490-.5)*width:0)).addScaledVector(up,(y0+y1)/2+(matched?(192.5/377-.5)*height:0));
 camera.position.copy(target).add(new THREE.Vector3(...v));camera.lookAt(target);camera.left=-width/2;camera.right=width/2;camera.top=height/2;camera.bottom=-height/2;camera.zoom=1;camera.updateProjectionMatrix();controls.target.copy(target);draw();
}
function look(){scene.overrideMaterial=masked?maskMaterial:clay?clayMaterial:null;scene.background=masked?null:new THREE.Color('#e9e4d7');floor.visible=!masked;draw();}
function light(mode){const s={day:[2.8,1.65,.6,[-8,16,14],'#fff0d8'],neutral:[2.5,1.6,.6,[-10,20,15],'#ffffff'],grazing:[3,1.25,.45,[-14,5,8],'#ffe8cc']}[mode];[key.intensity,hemi.intensity,fill.intensity]=s;key.position.set(...s[3]);key.color.set(s[4]);draw();}
function explode(on){exploded=on;const compiled=on?new Map(buildCafeSurfaces(cells,palette,{finish:finishMode!=='grid',isolated:true}).map(m=>[m.part,m])):null;for(const mesh of root.children){mesh.position.set(0,0,0);mesh.userData.assembledGeometry??=mesh.geometry;if(on&&!mesh.userData.explodedGeometry)mesh.userData.explodedGeometry=geometry(compiled.get(mesh.name));mesh.geometry=on?mesh.userData.explodedGeometry:mesh.userData.assembledGeometry;}const center=new THREE.Box3().setFromObject(root).getCenter(new THREE.Vector3());for(const mesh of root.children)mesh.position.copy(boxes.get(mesh.name).getCenter(new THREE.Vector3()).sub(center).multiplyScalar(on?.2:0));fit($('angle').value);}
function finish(mode){explode(false);finishMode=mode;rebuild();$('finish').value=mode;$('explode').setAttribute('aria-pressed','false');fit($('angle').value);}
$('finish').onchange=()=>finish($('finish').value);
function download(name,url){const a=document.createElement('a');a.download=name;a.href=url;a.click();}
$('angle').onchange=()=>fit($('angle').value);$('orbit').onclick=()=>{interactive=!interactive;controls.enabled=interactive;$('orbit').setAttribute('aria-pressed',String(interactive));};controls.addEventListener('change',draw);
$('mask').onclick=()=>{masked=!masked;look();};$('clay').onclick=()=>{clay=!clay;$('clay').setAttribute('aria-pressed',String(clay));look();};$('light').onchange=()=>light($('light').value);$('explode').onclick=()=>{explode(!exploded);$('explode').setAttribute('aria-pressed',String(exploded));};
$('save').onclick=()=>download(`cafe-detail-${$('angle').value}.png`,renderer.domElement.toDataURL('image/png'));
$('sheet').onclick=()=>{const c=document.createElement('canvas');c.width=1960;c.height=814;const x=c.getContext('2d');x.fillStyle='#eeece5';x.fillRect(0,0,c.width,c.height);x.drawImage($('reference'),0,60,980,754);x.drawImage(renderer.domElement,980,60,980,754);x.fillStyle='#393e38';x.font='24px sans-serif';x.fillText('Reference — exact crop',24,40);x.fillText('Cafe — individual reconstruction',1004,40);download('cafe-comparison.png',c.toDataURL());};
$('bundle').onclick=()=>{document.getElementById('proof')?.remove();explode(false);clay=false;const proof=document.createElement('details');proof.id='proof';proof.open=true;const summary=document.createElement('summary');summary.textContent='検証画像';proof.append(summary);function capture(name){const figure=document.createElement('figure'),img=document.createElement('img'),caption=document.createElement('figcaption');img.src=renderer.domElement.toDataURL('image/png');img.alt=name;caption.textContent=name;figure.append(img,caption);proof.append(figure);}
 light('day');masked=false;finish('texture');for(const view of ['match','front','right','back','left']){fit(view);look();capture(`finish-${view}`);}
 fit('match');for(const mode of ['grid','bevel','texture']){finish(mode);look();capture(`finish-${mode}`);}for(const mode of ['neutral','grazing']){light(mode);look();capture(`finish-${mode}`);}light('day');look();document.body.append(proof);};
$('export').onclick=()=>{const u=URL.createObjectURL(new Blob([JSON.stringify({format:'dayorama-voxel-cafe',version:2,cellSize:CELL,palette:PALETTE,cells:[...cells.values()],finishUnits:cells.finishUnits,finishRecipe:'surface-finish-v1',parts:root.userData.sculptRuntime.parts,source:'references/crops/cafe.png',qualityStatus:'prototype; formal visual gates pending'})],{type:'application/json'}));download('dayorama-cafe.voxels.json',u);setTimeout(()=>URL.revokeObjectURL(u),10000);};
const ray=new THREE.Raycaster(),pointer=new THREE.Vector2();let down;renderer.domElement.addEventListener('pointerdown',e=>down=[e.clientX,e.clientY]);renderer.domElement.addEventListener('pointerup',e=>{if(!down||Math.hypot(e.clientX-down[0],e.clientY-down[1])>5)return;const r=renderer.domElement.getBoundingClientRect();pointer.set((e.clientX-r.left)/r.width*2-1,-(e.clientY-r.top)/r.height*2+1);ray.setFromCamera(pointer,camera);const hit=ray.intersectObjects(root.children)[0];$('selected').textContent=hit?(PART_NAMES[hit.object.name]||hit.object.name):'';});
$('status').textContent=`${cells.size.toLocaleString()}ボクセル · ${triangles.toLocaleString()}三角形 · ${root.children.length}部材`;look();fit();
