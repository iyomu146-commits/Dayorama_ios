# Dayorama — 表現の比較 / 2026-09-23

共通の原案から、編集可能なボクセルと、形に合わせた部品のミニチュアを制作した独立した比較試作。本編への方式採用・移行は未決定。

現在は左に「窓と敷地」を表示する。「左の作品」で「前回の版」「初回」へ戻せる。右は初回の部品版。カメラ・一粒の寸法・光・露出は共通だが、作り込み版では形と配色を変更しているため、両画面を「同一形状の方式差だけ」とは扱わない。

「窓と敷地」は、不透明ガラスを壁から奥へ引き込み、周囲の壁・内枠・窓台と小鉢を立体化した版。中央の支柱はない。ガラスの反射模様は編集可能なセルの色で、写真投影や実際の店内ではない。敷地は浅い土の縁を持つ平らな輪郭へ変更し、大小の敷石で入口とテラスをつないだ。樹は枝が見える隙間を残し、葉の黄緑のコントラストを抑えている。

屋根は占有セルから連続した継ぎ目を作り、軒・棟を閉じた形にした。樹は幹・根・主枝・小枝をつなぎ、平たい葉の房を小さく不規則な樹冠へ変更。ベンチは5枚の座板、3枚の背板、肘掛け、座受けと横桟、椅子には座受けと貫を追加した。全て0.075mのセルとして保存される。窓は中央枠のない不透明ガラスを維持する。

`aigake` をWebルートとして配信し、`experiments/voxel-walk-lab-20260909/art-comparison-20260923/` を開く。外部API、外部CDN、課金サービスは利用しない。歩数・町の保存データを読み書きしない。

## 創作の循環を評価する

ユーザーが目指しているのは、一部の作者がボクセルで独自の形を作り込み、その高品質な作品を他の人が入手して歩行で建築する循環。用意した屋根・壁の組み合わせは、この自由な造形の代替にはならない。単体の完成図、建築途中、全景の読みやすさ、制作・改造の自由度、作者の作品を受け取る体験、端末負荷を分けて評価する。部品版の方がきれいという理由だけで移行を決めない。

## 操作

- 二画面のカメラが連動。単独表示、対象の切り替え、回転・拡大、背面、夜を確認できる。
- 共通の2万歩を基礎2,000、骨組み6,000、壁・窓12,000、屋根17,000、仕上げ20,000へ割り当てた。再生と手動スライダーがある。
- 左で一粒追加・削除・色変更、100操作まで戻す。完成状態で編集。回転は「眺める」へ戻す。
- 右は部品を離して確認でき、どちらも屋根色を変更できる。
- ボクセル作品はJSONへ書き出し、別の読み込みで同じ形を再現できる。途中の柱が完成後の壁で上書きされても、建築用の下層情報を保持する。
- 編集はこのページを開いている間のみ保持。残す場合は書き出す。読み込んだ作品は左だけに反映し、右が原案のままであることを表示する。
- 改修前・作り込み後・屋根色違い・読み込んだ作品の編集履歴は別々に保持し、切り替えで失わない。再読み込みで消えるため必要な作品は書き出す。対象の絞り込みは樹やベンチを単体で拡大する。

## 制作方式

原案は組み込み画像生成ツールで作成した。Meshyは使用していない。img2threejsの画像分析・構造仕様・厳格な仕様検査を使い、`design.mjs` に共通の寸法と部材を記述した。`render.mjs` はそれを直接立体化する手書きの描画処理であり、画像からの自動メッシュ抽出ではない。

ボクセルは1辺0.075mの一様格子。全てのセルを保持し、表示では隣接セルに隠れた面を省く。小物を非ボクセルの模型で置き換えて品質を補うことはしていない。細い線は連続したセルへ量子化し、薄い板は最低一粒を確保する。角の遮蔽をボクセルの頂点色へ反映。

部品は屋根の閉じた殻、壁の開口、枠、家具、枝、葉などの形状。表示時は同じ頂点属性を結合して描画負荷を抑え、選択用に部材と三角形の対応を保持する。描画の結合は編集用の設計情報を消さない。

色・光・露出・カメラは共通。窓は既存方針に合わせて不透明な色ガラス。中央の縦枠なし。原案に描かれた室内、樹皮・漆喰の細かな質感は未再現。原案との画像診断では形・画角の一致は合格基準未達で、img2threejs全工程の高忠実度合格とは扱わない。今回の成果は比較用の動作試作であり、両方式の最終品質の上限を示すものではない。

## 検証

`node --test design.test.mjs refined.test.mjs scene.test.mjs` : 17テスト。

屋根の欠損、垂木の屋根貫通、窓の消失、家具の接地と入口の通路、建築中の柱、全段階の進行、共有往復、座標・色・重複・形式の不正値を確認。ブラウザでも追加・戻す・JSON書き出し・再読み込み、前後からの形、骨組み、夜景を確認。

初回モデルは1,414部材、175,647セル、217,524三角形。作り込み版は2,056部材、175,366セル、213,088三角形（昼・完成・青緑屋根）。追加テストで屋根の被覆と継ぎ目の接触、樹の根元からの26近傍連結、ベンチの連結、入口の通路、色違い・編集・ガラス発光・建築段階の共有往復を確認。三角形数は影描画を重複して数えない。iPhone実機のFPS、熱、メモリ、地区全体への増殖、複雑な投稿作品の負荷上限は未検証。セル数だけで実機性能が同じとは判断しない。

`refinement-spec.json` は今回の形状変更の契約、`refined.mjs` はボクセル用の部材と格子上の仕上げ。`sculpt-spec.json` は初回の共通原案仕様のまま。img2threejsの完全な段階検収や原案への高忠実度合格を示す変更ではない。

「窓と敷地」の仕様は `scene-spec.json`、組み立ては `scene.mjs`。完成時144,042セル・211,956三角形。総セル数には地面・樹・小物と見えない内部を含み、建築途中だけ表示する下層は重複して足さない。窓のくぼみ、全ての脚と鉢の接地、入口を植栽が塞がないことも検査する。入口脇の鉢は通路確保のため横へ0.36m移動した。新しい版でも旧作品形式の読み書きと各版の編集保持を維持する。

この作品形式は比較専用。旧ワークショップ形式や本編には未接続。公開ギャラリー、アカウント、作者への帰属、通報・審査、配信容量の仕組みもこの試作には含めない。

## 密度・範囲編集・5棟の比較

`density.html` は旧比較を残した独立試作。7.5cm / 15cmと、表面そのまま / 平面結合を切り替える。形の保存と描画を分離し、色・発光・頂点AOが一致する平面だけを結合する。細かい版の形や色を軽量化のために変更しない。陰影が不均一な面は結合しない。

`density.html?mode=blank` で「ゼロから作る」を直接開く。4.8m四方の同じ作業台で、7.5cmは64×64、15cmは32×32。作品は0粒から始まり、作業台・グリッドは保存される作品セルに含めない。床を押して最初の粒を置き、ブロックの上面・側面へ積める。「1粒消す」「1粒塗る」、既存の範囲編集・対称・複製も使える。範囲追加は空の床でも2点を選択できる。範囲操作は「タップした場所に合わせる」が基本。床のタップで1段目、既存ブロックのタップでその直上の段を選び、2回目のタップで反対の角を指定する。配置予定を見て「ここに積む」で確定。段を直接選ぶ場合は「段を指定する」を使う。水平ガイド上の選択で、上下の段は変更しない。従来の立体範囲は「立体（2点間）」で選べる。段・範囲方式・粗さの変更時は未確定の選択を解除する。

白紙作品では範囲の追加先に直下のブロックが必要（1段目は床で支える）。支えがない部分は赤く予告して除外し、全て支えがなければ確定できない。一粒の側面追加は地面につながる形なら可能なので、庇などの張り出しを作れる。複製・対称でも孤立した追加は除外し、既に支えられている部分が浮く削除は拒否する。既存ファイル内の浮遊部分の自動修正やカフェ見本への支え制約は含まない。

「積む・消す・塗る」に共通の「範囲」切り替えを用意。「範囲」がオンなら2点で選択して確定し、オフなら一粒ずつ操作する。操作を変えても範囲のオン/オフは維持し、未確定の選択だけ解除する。「見る」の間も範囲設定を記憶する。操作ボタン、色パレット、画面内の確定と取り消しは44px以上の操作領域。段数入力、立体範囲、幅、複製、対称、ファイル操作も保持する。プレビューは作品・履歴に含めず、適用するまでセルを変更しない。

粗さごとの白紙作品とカフェ見本は別の状態・取り消し履歴を持つ。切り替え時の自動縮約や上書きはしない。「作業台を空に」は選択中の白紙作品のみ対象で、取り消し可能。作者が置ける範囲は作業台内・高さ4.8mまでで、ブラシや複製がはみ出す分は切り取る。作品形式には後方互換の `workspace` を追加し、白紙作品の読み込み先と範囲を区別する。旧形式はカフェ見本として読み込める。

白紙モードではドラッグで回転、拡大縮小ボタン・ホイール・ピンチで拡大、右ドラッグまたは2本指で移動。「全体」で視点を戻す。「上から」で範囲を選びやすい視点へ切り替える。複数指のジェスチャーやドラッグを終えた際に粒を誤配置しない。ページを閉じる前にJSONを書き出す（自動保存はない）。カフェの生成は見本を初めて開いたときまで遅延する。

| 敷地1組 | 保持セル | 建物・テラス | 表面の三角形 | 結合後の三角形 |
| --- | ---: | ---: | ---: | ---: |
| 7.5cm | 144,042 | 66,527 | 211,956 | 105,998 |
| 15cm | 25,074 | 11,773 | 50,236 | 26,874 |

数字はカフェ・樹・地面・小物の同じ1組。結合しても編集データのセル数は変わらない。15cm版は占有を残す縮約で、主屋幅64→32粒。家具・軒の細い部材が太り、ガラスの反射模様が粗くなる。この表の15cm版は自動縮約のまま。15cm格子で直接設計した別の喫茶店を `coarse.html` に追加（下記）。

建物単体で2点の範囲選択、塗装、充填、削除、オフセット複製、1/3/5粒幅の面追加、X=0を基準とする左右対称、30操作の取り消しができる。自由なセル造形を保持し、既製パーツの組み替えにはしていない。範囲は1操作40,000粒・作品240,000粒まで。7.5cm/15cmの編集は別々に保持。独立形式 `dayorama-density-study` のJSONで保存/読み込み。本編や旧比較との作品形式変換は含まない。

5棟の地区では同じ模型を5つ、樹5本、植物・家具、簡易住民12人を配置。完成した建物は形状を共有し、手前右の1棟だけ0〜20,000歩で建築する。住民は道路を周回し、夜は非表示。これは負荷検証用で、異なる建築物5種類や本編の生活行動の完成版ではない。

占有・工程・範囲編集・面作成をWorkerで実行。24粒単位の空間区画ごとに形状を保持し、編集や建築で変わった区画と隣接AOだけ作り直す。完成4棟は同じ結合済み形状を共有。カメラ回転だけでは静的な影を再生成せず、住民の影は最大10回/秒で更新。初期生成・密度変更時は全体を作り直す。

「25秒測る」は同じ画角・自動回転で完成10秒→建築15秒。結果は現在端末の画面更新間隔からのFPS、p95フレーム間隔、50ms超の数、Worker形状作成と往復応答時間。頂点量はアクティブ区画と共有模型の属性バッファのみで、全メモリ使用量ではない。GPU時間・発熱・電池消費は未計測。初期生成は測定外。背景タブ等の描画制限がある結果を端末性能として評価しない。iPhone実機評価はiPhoneでこの測定を実行する必要がある。

`node --test density.test.mjs` の11件で面結合の面積・向き、区画境界、AO更新範囲、複製/対称/戻す、縮約前後の骨組み、実模型の屋根被覆、JSON往復と不正値を検査する。白紙モードの0粒開始、積み上げ、密度と見本の分離、全消去の取り消し、作業台外への編集/不正な読み込みも対象。両密度で2段目のみを追加・塗装・削除して上下のセルを保つこと、取り消し、最上段と範囲外の指定も検査する。空中への追加、部分的な支え、複製/対称の支え、支えの削除、非破壊プレビューも対象。ブラウザー確認と制限は `evidence/density-review.md`。

## 次の評価

1. 作者が一粒ずつ整えたボクセルの屋根・葉・家具を、この自動量子化版からどこまで改善できるか。
2. 現在の素材感を超え、原案の奥行きと木・石・布の違いをどこまで保てるか。
3. 同じ質の建物を複数並べた全景と、実機での建築再生。
4. 編集・共有の価値も合わせた方式選定。現時点ではボクセルも第一候補から外さない。

## 15cmの喫茶店 — 2026-09-23

`coarse.html` で15cmの格子に直接設計した喫茶店と以前の縮約版を切り替える。光・材質・カメラは共通。主屋は同じ幅32粒だが、敷地と建物の形は異なる。新作は切妻屋根、周縁だけの深い窓枠、奥まった入口、レンガの腰壁、布庇、建物横のテーブルと椅子、少数の植木鉢を含む。画像を貼る方式・別の細かいメッシュは使わず、全て15cm立方体として編集可能。

新作は `coarse-cafe.mjs` が生成する。家具・窓・屋根を縮約せず格子で定義し、建築の各段階は既存の支えから隣接順に表示する。2万歩の基礎→骨組み→壁・窓→屋根→仕上げを維持。全ての表示途中で接地しているセルへ面接続するよう検査する（実際の強度計算ではない）。

「編集する」は `density.html?model=crafted`。範囲編集・取り消し・JSON書き出し／読み込みは従来と共通。7.5cmへの切り替えは各セルを8分割するだけで細部を自動追加しない。旧作と新作の編集状態は別に保持する。本編・iOSバンドルにはまだ採用せず、GitHubに比較用のソースとして含める。

検査: `node --test coarse-cafe.test.mjs density.test.mjs`。屋根の被覆、ガラス前の障害物、入口、家具の4本脚、全建築順の接地、ファイル往復を対象とする。ブラウザー確認は `evidence/coarse-review.md`。
